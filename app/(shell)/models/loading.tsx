import { ModelsSkeleton } from "@/features/models/models-skeleton";

/** The wait here is OpenRouter's catalog, a real third-party round trip. */
export default function ModelsLoading() {
  return <ModelsSkeleton />;
}
