import { LeaderboardSkeleton } from "@/features/leaderboard/leaderboard-skeleton";

/** The standings query is the wait feature 4 named when it allowed skeletons. */
export default function LeaderboardLoading() {
  return <LeaderboardSkeleton />;
}
